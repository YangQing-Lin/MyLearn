#### stack

-----------

栈

```c++
#include <iostream>
#include <cstring>
#include <algorithm>
#include <cstdio>
#include <stack> // stack 在此文件夹里

using namespace std;

int main() {
    // 常用函数
    push(); // 向栈顶插入一个元素
    top(); // 返回栈顶元素
    pop(); // 弹出栈顶元素
    
    return 0;
}
```

